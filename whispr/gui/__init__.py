"""Tkinter GUI for Whispers, split into focused modules.

WORK IN PROGRESS: the shared widgets, error helpers, transcript view and
Translate tab have been extracted here; the app shell (``gui.app``), the
Transcribe tab, and the ``whispr.app`` re-export shim are still to be wired up.
Until then the live GUI continues to run from ``whispr.app``.
"""
